services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: eox
      POSTGRES_PASSWORD: eox
      POSTGRES_DB: eox
    ports:
      - "5433:5432"
    volumes:
      - eox-pgdata:/var/lib/postgresql/data

volumes:
  eox-pgdata:
